<Window
  x:Class="MermaidPad.Views.Dialogs.ExportDialog"
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:vm="clr-namespace:MermaidPad.ViewModels.Dialogs"
  Title="Export Diagram"
  MinHeight="400"
  MaxWidth="550"
  MaxHeight="600"
  d:DesignHeight="800"
  d:DesignWidth="525"
  x:DataType="vm:ExportDialogViewModel"
  CanResize="False"
  ShowInTaskbar="False"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  mc:Ignorable="d">

  <Window.Styles>
    <Style Selector="TextBlock.SectionHeader">
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Margin" Value="0,10,0,5" />
      <Setter Property="FontSize" Value="14" />
    </Style>

    <Style Selector="Border.SectionBorder">
      <Setter Property="BorderBrush" Value="#E0E0E0" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="4" />
      <Setter Property="Padding" Value="10" />
      <Setter Property="Margin" Value="0,5,0,10" />
    </Style>

    <Style Selector="Grid.OptionRow">
      <Setter Property="Margin" Value="0,5" />
    </Style>

    <Style Selector="TextBlock.OptionLabel">
      <Setter Property="Width" Value="100" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
  </Window.Styles>

  <Design.DataContext>
    <vm:ExportDialogViewModel />
  </Design.DataContext>

  <Grid Margin="5" RowDefinitions="Auto,*,Auto">

    <!--  Header  -->
    <StackPanel Grid.Row="0">
      <TextBlock
        Margin="0,0,0,10"
        FontSize="18"
        FontWeight="Bold"
        Text="Export Diagram" />

      <TextBlock
        Margin="0,0,0,20"
        Foreground="#666666"
        Text="Configure export settings for your diagram" />
    </StackPanel>

    <!--  Main Content  -->
    <ScrollViewer
      Grid.Row="1"
      Padding="5,0,0,0"
      HorizontalScrollBarVisibility="Disabled"
      VerticalScrollBarVisibility="Auto">
      <StackPanel Margin="0,0,20,0">
        <!--  File Settings  -->
        <TextBlock Classes="SectionHeader" Text="File Settings" />
        <Border Classes="SectionBorder">
          <StackPanel>
            <!--  File Name  -->
            <Grid Classes="OptionRow">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock
                Grid.Column="0"
                Classes="OptionLabel"
                Text="File Name:" />
              <TextBox
                Grid.Column="1"
                Text="{Binding FileName}"
                Watermark="diagram" />
            </Grid>

            <!--  Format  -->
            <Grid Classes="OptionRow">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <TextBlock
                Grid.Column="0"
                Classes="OptionLabel"
                Text="Format:" />
              <ComboBox
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                ItemsSource="{Binding AvailableFormats}"
                SelectedItem="{Binding SelectedFormat}">
                <ComboBox.ItemTemplate>
                  <DataTemplate DataType="vm:ExportFormatItem">
                    <TextBlock Text="{Binding Description}" />
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
              <Button
                Grid.Column="3"
                Width="100"
                VerticalAlignment="Center"
                Command="{Binding BrowseForDirectoryCommand}"
                CommandParameter="{Binding StorageProvider, RelativeSource={RelativeSource AncestorType=Window}}"
                Content="Browse..." />
            </Grid>

            <!--  Directory  -->
            <Grid Classes="OptionRow">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock
                Grid.Column="0"
                Classes="OptionLabel"
                Text="Save Location:" />
              <TextBox
                Grid.Column="1"
                IsReadOnly="True"
                Text="{Binding Directory}" />
            </Grid>
          </StackPanel>
        </Border>

        <!--  PNG Options (visible when PNG is selected)  -->
        <StackPanel IsVisible="{Binding IsPngSelected}">
          <TextBlock Classes="SectionHeader" Text="PNG Options" />
          <Border Classes="SectionBorder">
            <StackPanel>
              <!--  DPI  -->
              <Grid Classes="OptionRow">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                  Grid.Column="0"
                  Classes="OptionLabel"
                  Text="DPI:" />
                <ComboBox
                  Grid.Column="1"
                  Width="100"
                  ItemsSource="{Binding AvailableDpiValues}"
                  SelectedItem="{Binding SelectedDpi}">
                  <ComboBox.ItemTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding StringFormat='{}{0} DPI'}" />
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock
                  Grid.Column="3"
                  VerticalAlignment="Center"
                  Text="(Higher = better quality, larger file)" />
              </Grid>

              <!--  Scale Factor  -->
              <Grid Classes="OptionRow">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                  Grid.Column="0"
                  Classes="OptionLabel"
                  Text="Scale Factor:" />
                <Slider
                  Grid.Column="1"
                  IsSnapToTickEnabled="True"
                  Maximum="4"
                  Minimum="1"
                  TickFrequency="1"
                  Value="{Binding ScaleFactor}" />
                <TextBlock
                  Grid.Column="3"
                  Width="40"
                  VerticalAlignment="Center"
                  Text="{Binding ScaleFactor, StringFormat='{}{0:F1}x'}" />
              </Grid>

              <!--  Background  -->
              <Grid Classes="OptionRow">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="20" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                  Grid.Column="0"
                  Classes="OptionLabel"
                  Text="Background:" />
                <RadioButton
                  Grid.Column="1"
                  Content="White"
                  GroupName="Background"
                  IsChecked="{Binding UseWhiteBackground}" />
                <RadioButton
                  Grid.Column="3"
                  Content="Transparent"
                  GroupName="Background"
                  IsChecked="{Binding UseTransparentBackground}" />
              </Grid>

              <!--  Quality  -->
              <Grid Classes="OptionRow">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="10" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                  Grid.Column="0"
                  Classes="OptionLabel"
                  Text="Quality:" />
                <Slider
                  Grid.Column="1"
                  IsSnapToTickEnabled="True"
                  Maximum="100"
                  Minimum="70"
                  TickFrequency="5"
                  Value="{Binding Quality}" />
                <TextBlock
                  Grid.Column="3"
                  Width="40"
                  VerticalAlignment="Center"
                  Text="{Binding Quality, StringFormat='{}{0}%'}" />
              </Grid>

              <!--  Anti-aliasing  -->
              <Grid Classes="OptionRow">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                  Grid.Column="0"
                  Classes="OptionLabel"
                  Text="Anti-aliasing:" />
                <CheckBox
                  Grid.Column="1"
                  Content="Enable smooth edges"
                  IsChecked="{Binding AntiAlias}" />
              </Grid>

              <!--  Max Dimensions  -->
              <Expander Margin="0,10,0,0" Header="Advanced Settings">
                <StackPanel Margin="0,10,0,0">
                  <Grid Classes="OptionRow">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="150" />
                      <ColumnDefinition Width="5" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                      Grid.Column="0"
                      Classes="OptionLabel"
                      Text="Max Width:" />
                    <NumericUpDown
                      Grid.Column="1"
                      Increment="100"
                      Maximum="10000"
                      Minimum="0"
                      Watermark="0 = no limit"
                      Value="{Binding MaxWidth}" />
                    <TextBlock
                      Grid.Column="3"
                      VerticalAlignment="Center"
                      Text="pixels" />
                  </Grid>

                  <Grid Classes="OptionRow">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="150" />
                      <ColumnDefinition Width="5" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                      Grid.Column="0"
                      Classes="OptionLabel"
                      Text="Max Height:" />
                    <NumericUpDown
                      Grid.Column="1"
                      Increment="100"
                      Maximum="10000"
                      Minimum="0"
                      Watermark="0 = no limit"
                      Value="{Binding MaxHeight}" />
                    <TextBlock
                      Grid.Column="3"
                      VerticalAlignment="Center"
                      Text="pixels" />
                  </Grid>

                  <Grid Classes="OptionRow">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <CheckBox
                      Grid.Column="1"
                      Content="Preserve aspect ratio when limiting dimensions"
                      IsChecked="{Binding PreserveAspectRatio}" />
                  </Grid>
                </StackPanel>
              </Expander>
            </StackPanel>
          </Border>
        </StackPanel>

        <!--  SVG Options (visible when SVG is selected)  -->
        <StackPanel IsVisible="{Binding IsSvgSelected}">
          <TextBlock Classes="SectionHeader" Text="SVG Options" />
          <Border Classes="SectionBorder">
            <StackPanel>
              <CheckBox Content="Optimize SVG (remove unnecessary elements)" IsChecked="{Binding OptimizeSvg}" />
            </StackPanel>
          </Border>
        </StackPanel>

        <!--  Preview  -->
        <TextBlock Classes="SectionHeader" Text="Preview" />
        <Border Classes="SectionBorder">
          <StackPanel>
            <TextBlock Margin="0,0,0,10" Text="Output File:" />
            <TextBox
              FontFamily="Consolas,Monaco,monospace"
              IsReadOnly="True"
              Text="{Binding FullFilePath}" />

            <Grid Margin="0,10,0,0" IsVisible="{Binding HasDimensionInfo}">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35*" />
                <ColumnDefinition Width="65*" />
              </Grid.ColumnDefinitions>

              <StackPanel Grid.Column="0">
                <TextBlock
                  Margin="0,0,0,10"
                  FontWeight="SemiBold"
                  Text="Estimated Dimensions:" />
                <TextBlock
                  Margin="0,0,0,10"
                  FontWeight="SemiBold"
                  Text="Estimated File Size:" />
              </StackPanel>

              <StackPanel Grid.Column="1">
                <TextBlock
                  Margin="0,0,0,10"
                  Foreground="#666666"
                  Text="{Binding EstimatedDimensions}" />
                <TextBlock
                  Margin="0,0,0,10"
                  Foreground="#666666"
                  Text="{Binding EstimatedFileSize}" />
              </StackPanel>
            </Grid>
          </StackPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>

    <!--  Buttons  -->
    <StackPanel
      Grid.Row="2"
      Margin="0,10,0,0"
      HorizontalAlignment="Right"
      Orientation="Horizontal"
      Spacing="10">
      <Button
        Width="100"
        Classes="accent"
        Command="{Binding ExportCommand}"
        Content="Export"
        IsDefault="True" />
      <Button
        Width="100"
        Command="{Binding CancelCommand}"
        Content="Cancel"
        IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
